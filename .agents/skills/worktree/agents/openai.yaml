interface:
  display_name: "Worktree"
  short_description: "Create project worktrees for Codex sessions"
  default_prompt: "Use $worktree to create a new worktree for this session."
policy:
  allow_implicit_invocation: false
